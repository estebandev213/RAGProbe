# --- Project-specific ---
# Build plan / agent instructions — not part of the published repo
CLAUDE.md

# --- Secrets & env ---
# Ignore all .env variants (.env, .env.local, .env.production, ...) except the
# checked-in template. Real credentials must never be committed.
.env
.env.*
!.env.example

# --- Python ---
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend ---
node_modules/
dist/

# --- Data / runtime ---
data/
*.db

# --- OS / editor ---
.DS_Store
